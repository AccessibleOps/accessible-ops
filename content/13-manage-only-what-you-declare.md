---
title: "Ownership"
slogan: "Manage only what you declare"
slug: "manage-only-what-you-declare"
weight: 13
roman: "XIII"
part: "factor"
summary: "The tool changes only declared fields and deletes only what carries its ownership marker, so an omission is never a purge."
benefits: ["confidence", "repeatability"]
---

A half-written config says nothing about what it leaves out. The tool changes only declared fields and deletes only what carries its ownership marker, so an omission is never a purge.
